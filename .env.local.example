# Razorpay (Payments)
# Get these from Razorpay Dashboard > Settings > API Keys
RAZORPAY_KEY_ID=rzp_test_Rpov95B9gi83WT
RAZORPAY_KEY_SECRET=NSgloxllrxu0dQwv0JwCHIEu

# Supabase (Database)
# Get these from Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://awtyyaayqyyocxeoyhsk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImF3dHl5YWF5cXl5b2N4ZW95aHNrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjUyNzk5NjgsImV4cCI6MjA4MDg1NTk2OH0.L8kVKCdgTPgScvOGNVYHbJDrsNpJzzOY4avL-79yMv0

# Admin & Security
# Generate a long random string for ADMIN_SECRET
ADMIN_SECRET=change_this_to_a_long_random_value
# Base URL for the application (default localhost for dev)
BASE_URL=http://localhost:3000

# Optional: Email (SMTP)
# Only needed if you want to replace the default emailer
SMTP_USER=yourgmailaddress@gmail.com
SMTP_PASS=your_app_password_here
EMAIL_FROM=noreply@yourdomain.com

query = query.or(`id.ilike.%${q}%,email.ilike.%${q}%`, { foreignTable: 'tickets' })

const email = (metadata?.email || '').trim()
if (!name || !email) return res.status(400).json({ error: 'missing_user_details' })

const { data: ticket, error } = await supabase.from('tickets').select('*').eq('id', id).single()
if (ticket?.used) return res.json({ valid: false, reason: 'already_used' })
await supabase.from('tickets').update({ used: true, used_at: new Date() }).eq('id', id)

const supabase = createClient(process.env.SUPABASE_URL || '', process.env.SUPABASE_SERVICE_KEY || '')
